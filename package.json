{
	"name": "reactive3",
	"version": "0.0.1",
	"description": "一款简单的响应式MVVM框架",
	"main": "dist/reactive.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"serve": "webpack --mode development",
		"build": "webpack --mode production"
	},
	"author": "lingkai",
	"license": "MIT",
	"devDependencies": {
		"babel-core": "^6.26.3",
		"babel-loader": "^7.1.5",
		"babel-preset-env": "^1.7.0"
	},
	"babel": { 
		"presets": [
			"env"
		],
		"plugins": []
	}
}
